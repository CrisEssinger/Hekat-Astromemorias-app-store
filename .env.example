# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# PagBank Configuration
# Token de acesso (Obtenha no painel do PagBank)
PAGBANK_TOKEN=
# Ambiente: sandbox ou production
PAGBANK_ENV=sandbox
# Chave pública para criptografia de cartão no frontend (se necessário)
VITE_PAGBANK_PUBLIC_KEY=

# Integração Externa (Cia do Céu)
# Chave secreta para autorizar o webhook externo (Crie uma senha forte)
EXTERNAL_WEBHOOK_SECRET=

# Firebase Client Configuration (Optional overrides for external production like Vercel)
# If not specified, the app falls back to the local sandboxed project configuration.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_DATABASE_ID=

